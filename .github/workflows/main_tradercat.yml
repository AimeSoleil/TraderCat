# Docs for the Azure Web Apps Deploy action: https://github.com/azure/functions-action
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure Functions: https://aka.ms/python-webapps-actions

name: Build and deploy Python project to Azure Function App - tradercat

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_PACKAGE_PATH: '.' # set this to the path to your web app project, defaults to the repository root
  PYTHON_VERSION: '3.12' # set this to the python version to use (supports 3.6, 3.7, 3.8)

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python version
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate

      - name: Install dependencies
        run: pip install -r requirements.txt

      # Optional: Add step to run tests here

      - name: Zip artifact for deployment
        run: zip release.zip ./* -r

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            release.zip
            !venv/
  # I give-up azure function dpeloyment currently as I cannot find my functions in azure portal after so many times successful deployment.
  # You can try to enable the following deploy job if you want.
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   permissions:
  #     id-token: write #This is required for requesting the JWT
  #     contents: read #This is required for actions/checkout

  #   steps:
  #     - name: Download artifact from build job
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: python-app

  #     - name: Unzip artifact for deployment
  #       run: |
  #         unzip release.zip
  #         rm release.zip
        
  #     - name: Login to Azure
  #       uses: azure/login@v2
  #       with:
  #         client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_E3E67C316AEC4CC2988BCDECC3BB581A }}
  #         tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_7B41B10852B74557AC3685C68F70DCD9 }}
  #         subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_CAB70194D89E4E68B4D8FCBAE1DD4973 }}

  #     - name: 'Deploy to Azure Functions'
  #       uses: Azure/functions-action@v1
  #       id: deploy-to-function
  #       with:
  #         app-name: 'tradercat'
  #         slot-name: 'Production'
  #         package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          